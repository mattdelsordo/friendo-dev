-
    var stats = [
    {
    name: 'Core',
    icon: '\u{1f455}',
    trainDesc: 'Do some sit ups?',
    verb: 'Crunch'
    },
    {
    name: 'Leg',
    icon: '\u{1f9b5}',
    trainDesc: 'Do some squats?',
    verb: 'Squat'
    },
    {
    name: 'Arm',
    icon: '\u{1f4aa}',
    trainDesc: 'Do some curls?',
    verb: 'Curl'
    },
    {
    name: 'Sight',
    icon: '\u{1f441}',
    trainDesc: 'Read a book?',
    verb: 'Read'
    },
    {
    name: 'Hair',
    icon: '\u{1f488}',
    trainDesc: 'Brush hair?',
    verb: 'Groom'
    },
    {
    name: 'Taste',
    icon: '\u{1f445}',
    trainDesc: 'Do some extreme eating?',
    verb: 'Munch'
    },
    {
    name: 'Dog Affinity',
    icon: '\u{1f415}',
    trainDesc: 'Play with a dog?',
    verb: 'Get licked'
    },
    {
    name: 'Meme Tolerance',
    icon: '\u{1f9e0}',
    trainDesc: 'Scope some dank memes?',
    verb: 'Surf the web'
    }
    ]

-
    var egginfo = {
    name: 'Egg',
    icon: '\u{1f95a}',
    trainDesc: 'Incubate the egg?',
    verb: 'Incubate'
    }

mixin statdisplay(stat)
  - var statId = stat.name.split(' ')[0].toLowerCase()
  .d-flex.flex-row.align-items-center.stat-row.p-2(data-toggle='modal' data-target=`#train-${statId}` id=`${statId}-bar`)
      span.mr-1(id=`${statId}-icon`) #{stat.icon}
      span.mr-1(id=`${statId}-name`) #{stat.name}
      .progress.ml-auto.w-50
          .progress-bar.bg-info(id=`${statId}-prog` role=progressbar)
      span.ml-1.p-2.statlvl(id=`${statId}-num` style='white-space: pre-wrap;') 000

mixin trainingDialog(stat)
    - var statId = stat.name.split(' ')[0].toLowerCase()
    .modal.fade(id=`train-${statId}` role='dialog')
        .modal-dialog(role='document')
            .modal-content
                .modal-header
                    h5.modal-title Train #{stat.name}
                .modal-body
                    h6 #{stat.trainDesc}
                    div
                        label(for=`${statId}-trainbtn`)
                            | #{stat.verb} for
                            input.mr-1.ml-1(id=`${statId}-trainamnt` type='number' step=0.2 min=0)
                            | minutes
                .modal-footer
                    button.btn.btn-secondary(data-dismiss='modal') Cancel
                    button.btn.btn-success(id=`start-${statId}`) Begin

.row
    .col
        .hidden#egg-display
            +statdisplay(egginfo)
            +trainingDialog(egginfo)
        #stat-display
            h5 Stats
            each stat in stats
                +statdisplay(stat)
                +trainingDialog(stat)
